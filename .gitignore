node_modules/
dist/
/build/
# Per-Thread Job workspaces, the wrapper's Session mapping, and the default Vault:
# all instance state, none of it the project's.
/.workspaces/
/.state/
/vault/
.env
.env.*
!.env.example
# This instance's configuration. It holds no credentials — only their variable names — so
# it is safe to commit in your *own* fork, and it is not this project's to ship.
/open-agent.config.json
/mcp.json
*.log
.DS_Store
.claude/settings.local.json
/.test-tmp/
